# 📖 Инструкция по управлению материалами и тестами (через бота)

## 🎯 Уровни сложности
- **🔰 Базовый** — для начинающих
- **⚡ Средний** — для продолжающих
- **🔥 Продвинутый** — для опытных

## 🔐 Права
Админ-команды доступны только администраторам (`утилиты.auth.is_admin`).

## ➕ Добавление материала (бот)
1. Отправьте `/add_material`
2. Введите название → текст (несколько сообщений, завершите `/done`)
3. Выберите уровень (кнопки)
4. При необходимости добавьте вопросы сразу или позже

## ✏️ Редактирование
- `/edit_material` → выбрать материал → действие:
  - изменить название
  - дополнить текст
  - добавить/заменить видео

## ❓ Вопросы к тестам
- `/add_question` → выбрать материал → ввести вопрос → варианты через запятую → номер правильного
- В процессе `/add_material` можно сразу добавлять вопросы

## 🗑️ Удаление
- `/delete_material` → выбрать материал → подтвердить удаление (вопросы/ответы удалятся автоматически)

## 📋 Списки
- `/list_materials` — список материалов (первые 20 штук)
- `/leaderboard` — топ-10 пользователей (публично)

## 🧑‍💻 Через Python (при необходимости)
```python
from утилиты.database import db

material_id = db.add_material(
    title="Название",
    text_content="Текст...",
    level="базовый",  # или "средний", "продвинутый"
)

question_id = db.add_question(material_id, "Вопрос?")
db.add_answer(question_id, "Правильный", True)
db.add_answer(question_id, "Неправильный 1", False)
db.add_answer(question_id, "Неправильный 2", False)
```

## ⚠️ Важные замечания
1. Уровни: `базовый`, `средний`, `продвинутый`
2. Индексы ответов в коде — с нуля; в боте — с 1
3. Удаление материала чистит связанные вопросы и ответы
4. Используйте `/list_materials` для просмотра ID

## 🆘 Помощь
- Все админ-команды вызывайте через Telegram-бота
- Для проверки прав — функция `is_admin` в `утилиты/auth.py`
- Для добовлене админов добавьте ID телеграм аккаунта в .env фаил ADMIN_IDS=123хххх
